import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Course Project 4.3',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
